import { mutations } from "./mutation";
import { queries } from "./query";
import { resolvers } from "./resolver";
import { typeDefs } from "./typedef";

export const User = { queries, mutations, typeDefs, resolvers };